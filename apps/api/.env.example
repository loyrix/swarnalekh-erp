# ===========================
# SwarnaLekh API — Environment Variables
# ===========================

# Temporary mobile auth provider config.
# The API does not require these; scripts/sync_mobile_env.mjs copies them to apps/mobile/.env.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# PostgreSQL database (Supabase pooler, AWS RDS, or any managed Postgres)
DATABASE_URL="postgresql://postgres.project-ref:password@region.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.project-ref:password@region.pooler.supabase.com:5432/postgres"

# First-party auth: the API BOTH signs (POST /auth/register, /auth/login) and
# verifies our own HS256 access tokens with this secret. Required — set a long
# random value (e.g. `openssl rand -base64 48`). Rotating it invalidates all
# issued tokens (users must log in again).
AUTH_JWT_SECRET=replace-with-a-long-random-secret
# Backward-compatible alias if your local env already uses JWT_SECRET.
JWT_SECRET=
# For RS256/ES256 providers, prefer a generic JWKS URL.
# Current Supabase-backed auth can omit this if SUPABASE_URL is set; the API derives:
# https://your-project.supabase.co/auth/v1/.well-known/jwks.json
AUTH_JWKS_URL=


# Redis (Upstash) — optional until queues are enabled
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=

# Gemini inventory OCR
GEMINI_API_KEY=
GEMINI_INVENTORY_MODEL=gemini-2.5-flash-lite

# App
NODE_ENV=development
PORT=3000
API_VERSION=v1
