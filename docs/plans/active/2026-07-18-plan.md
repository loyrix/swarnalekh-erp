# Plan — 18 July 2026

Requirements shared today. More coming — will be added below.

## Dashboard

- [ ] Add a "Business Overview" heading above the statistics cards (below Quick Actions).

## Inventory

- [ ] Product Name and Category ask for the same thing — remove Product Name, auto-fill it from the selected Category.
- [ ] Existing jewellery tag mapping (HUID import):
  - New jewellery keeps auto-generated tags (RG-01, PD-02, …).
  - On HUID import, don't force an auto tag — let the user enter/scan the existing physical tag per item.
  - Typing just a number ("2") auto-formats to the sequence ("PD-02").
  - Internal unique ID + visible tag are both stored; search works on both. (Already mostly supported — the missing part is the tag entry on the import screen.)
  - Barcode/QR scanning comes as a follow-up (needs a scanner library).

## Mortgage

- [ ] If the entered mobile number belongs to another customer, show an error — don't silently switch the customer name.
- [ ] Interest cycle must be exact date-to-date (10th → 10th). Next month starts only AFTER that date passes. Right now it flips a day early — fix.
- [ ] After a partial principal payment, next cycle's interest is on the REMAINING principal (₹50,000 − ₹10,000 → interest on ₹40,000).

## Billing

- [ ] Enter Gold Rate, Making Charges, and GST directly while creating the bill (walk-in). Bill calculates from those values.
- [ ] Invoice must include Customer Address (currently missing — needs a DB column + form field + PDF).
      Rest of the professional format (shop details, purity, weight, rate, making, GST, totals) shipped in the latest APK — verify on device.

## Reports

- [ ] Current Stock Report download must be a clean professional PDF. Plan: build report PDFs in the app with the same engine as the invoice (real tables + letterhead) — fixes all report types at once.

## Decisions (owner, 19 Jul)

1. Inventory: keep an optional Details field after removing Product Name. ✓
2. Tags: category-wise sequences, 4-digit numbering (RG-0001, PD-0001). New tags use
   this format; existing short tags (RG-05) stay valid.
3. Tag Number field also on the manual Add Item form; barcode scanner comes later. ✓
4. Mortgage: partial principal payment reduces interest from the NEXT cycle only;
   the current cycle's interest stays unchanged. ✓
5. Billing: one Gold Rate field applies to all rate-based items in the bill. ✓
6. Invoice: Customer Address is the only missing detail in the latest format. ✓

## Order of work (after discussion)

1. Quick wins: dashboard heading, inventory dedup, mortgage phone check
2. Mortgage interest fixes (date-to-date + remaining principal)
3. Billing: rate/making/GST inputs + customer address
4. Report PDFs
5. Tag mapping on HUID import

All questions answered 19 Jul. Ready to start on go.
