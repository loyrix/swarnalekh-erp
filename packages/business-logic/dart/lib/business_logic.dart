/// SwarnaLekh Business Logic
///
/// Pure business logic functions shared between the Flutter app and API.
/// All functions are pure: same inputs → same outputs, no side effects.
///
/// All monetary values in paise (integers).
/// All weights in milligrams (integers) for precision.
library business_logic;
