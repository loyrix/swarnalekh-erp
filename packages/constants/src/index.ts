/**
 * SwarnaLekh Constants
 *
 * Shared constants, enums, HSN codes, and karat values.
 * Used by both API and business-logic packages.
 *
 * Constants will be populated in Task 1.1 (setup constants package).
 */

export {};
