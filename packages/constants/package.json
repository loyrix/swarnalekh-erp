{
  "name": "@swarnbook/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Shared constants, enums, HSN codes, and karat values for SwarnaLekh",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
